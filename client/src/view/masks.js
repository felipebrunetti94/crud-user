export const cpfMask = {
  apply(cpf) {
    return cpf;
  },
  remove(masked) {
    return masked;
  },
};

export const accountMask = {
  apply(account) {
    return account;
  },
  remove(masked) {
    return masked;
  },
};

export const agencyMask = {
  apply(agency) {
    return agency;
  },
  remove(masked) {
    return masked;
  },
};
